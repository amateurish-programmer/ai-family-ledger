# V1.6.1 云端开发与部署流程验证

本版 versionName 1.6.1、versionCode 19，沿用原包名、Room 数据库和安装签名，可覆盖升级 V1.6.0。

## 变更范围

本版仅升级版本元数据和发布说明，不改变记账、同步、报告、AI、导入导出或更新逻辑，也不包含数据库迁移。目的是验证从仓库提交到 GitHub Actions 全量检查、版本化 APK 构建，再到 Supabase 应用内更新发布的完整云端流程。

## 验证计划

先执行发布脚本的本地合成 Storage 回归和静态差异检查。随后在 `main` 分支手动运行 **Android APK** 工作流并启用 `publish_release`；该模式强制执行 JVM 测试、后端协议与 PostgreSQL 权限测试、Android 35 模拟器测试、APK 构建和发布。只有所有前置任务成功才允许更新 Supabase 的 `latest.json`。

交付包名为 `AI家庭账本-v1.6.1.apk`，不生成本机 SHA-256 边车文件。云端运行、公开清单和 APK 的实际结果将在流程结束后补充；流程成功前不声明已经发布。

## 发布验证

- GitHub Actions 完整流程：[34849667885](https://github.com/amateurish-programmer/ai-family-ledger/actions/runs/34849667885) 的 `backend-tests`、`build`、`device-tests`、`publish` 均成功。
- PR 云端预检：[34849091149](https://github.com/amateurish-programmer/ai-family-ledger/actions/runs/34849091149) 的后端检查、JVM 测试、lint、APK 构建和 Artifact 上传均成功。
- Supabase 公共更新清单已更新为 `versionCode=19`、`versionName=1.6.1`、`packageName=com.familyledger.app`，声明 APK 大小 `17,930,673` 字节。
- 公共 APK：[ai-family-ledger-v1.6.1.apk](https://xdgeybztysuvvwagqkvb.supabase.co/storage/v1/object/public/app-updates/releases/19/ai-family-ledger-v1.6.1.apk)。独立下载后核对大小和 SHA-256 与公开清单一致，且 APK 必需 ZIP 条目齐全；未生成 SHA-256 边车文件。

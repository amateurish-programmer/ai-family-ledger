# V1.5.0 前台自动检查更新

本版 versionName 1.5.0、versionCode 17，沿用原包名、Room 数据库和安装签名，可覆盖升级。

## 更新检查

应用每次进入前台时调用现有公开 `latest.json` 清单检查版本。检查保持静默：无新版时不显示“已是最新版本”，网络或清单错误也不弹错误；检查过程不会自动下载或安装。发现高于当前版本的清单后，只有在账本加载、同步、编辑、导入导出等阻塞状态结束且界面恢复到前台时，才弹出“发现新版本”对话框。选择“查看更新”进入设置页沿用原有下载、校验和系统安装流程，选择“稍后”可关闭本次提示。

每次进入前台都允许新的自动检查，并用 ViewModel 的活动任务和已有更新状态防止同一前台事件重复请求或覆盖正在下载、校验、安装的状态。设置页手动检查继续绕过自动检查限制。

## 验证

新增 JVM 测试覆盖前台额度可重复领取，同时保留日期额度实现的历史兼容测试。Android UI 测试继续覆盖新版提示等待阻塞流程结束、稍后关闭不重播和不自动下载。由于本机没有 Java/Gradle，构建、JVM、lint、模拟器和发布检查由 GitHub Actions 执行；真实手机前台切换、弱网和覆盖安装仍需真机验收。

交付包名为 `AI家庭账本-v1.5.0.apk`，不生成本机 SHA-256 边车文件。

v1.5.0：应用每次进入前台静默检查更新；没有新版本或检查失败不提示，发现新版本才弹出更新说明并进入设置下载。手动检查更新不受影响。
确认记账、编辑、删除后立即同步家庭账本。
Excel导入与撤销、人情历史整理、备份恢复后也会立即同步。
同步结果只提示非零的上传、下载和冲突数量，无变化时不打扰。
网络失败仍保留本机数据，可稍后自动或手动重试。
v1.4.0：同步时仍可输入，发送完成后再提交；“对话”改为“记账”；角色、头像、家庭账号与同步集中到角色设置。
保留原账本、对话、人情数据和家庭配置，支持覆盖升级。

# 后续阶段 Implementation Plan

**Goal:** 连续交付 V0.3 语音/AI提案、V0.4 可配置家庭同步、V0.5 报告。
**Spec:** ../specs/2026-09-06-ledger-v03-v05-design.md
**Architecture:** QuickEntryCodec → 确认卡 → Repository；CloudService → Supabase RPC/CAS；LedgerReport → 报表与文件导出。
**约束:** 不自动记入 AI 输出；不在 APK 中存模型密钥；不跨家庭混账；保持用户 V0.1 数据；按用户最新指示不运行模拟器/专项验证。

- [x] QuickEntryCodec 简单解析与 AI JSON 校验；QuickEntryScreen 语音入口、提案编辑和统一保存。
- [x] LedgerReport 确定性汇总与报告导出，ReportsScreen 月度趋势/成员/AI入口。
- [x] CloudScreen 配置/登录/家庭/同步与冲突选择，ViewModel 串行写入与前台同步开关。
- [ ] 独立云端工作：SQL 表/RLS/RPC/邀请/CAS；CloudService Keystore+Auth+分页同步；Edge Function parse/report 与部署文档。
- [ ] 合并客户端/服务端接口，执行 APK 编译，处理编译错误。
- [ ] 发布源码与文档，下载 0.5.0 APK。记录 Supabase 未配置/未部署的外部边界。

# V0.9.0 · 对话保存、账号资料与日周报告

## 功能

- 对话与待确认提案写入 Room V3，按本机绑定账号分区，退出不清除，重新打开应用加载。确认提案、清空待确认状态与保存确认回复为同一事务；重复确认不重复入账。消息不再截断为最后八十条，发给模型的上下文仍有限。对话保存在本机，不自动同步到家人设备或放入账本 JSON 备份；未发送输入框文本暂不跨进程保存。
- Room 2→3 只新增对话表，不重建账目。旧版没有保存到数据库、已经消失的对话不能恢复。
- 忘记密码：发送邮箱验证码，输入验证码与两次新密码，通过独立 recovery 会话更新；不写入恢复令牌或替换本机绑定账号。成功返回登录；验证码失效、发送失败、弱密码和更新结果不确定有明确提示。详见 [身份说明](IDENTITY_V0.9.md)。
- 家庭名称和图标由家庭创建者修改，成员刷新或同步后可见；角色名称和头像在设置页编辑并按本机账号保存，不充当权限。家庭图标与角色头像为内置选择。
- 新增日报、周报；周一开始，跨月跨年正确；保留月/年报、前期比较、趋势、导出和 AI 解读。详见 [报告说明](REPORTS_V0.9.md)。
- 每次升级自动提交受检源码并构建。APK 与源码包从 Gradle versionName 自动命名，交付 `AI家庭账本-v0.9.0.apk`，不生成本机 SHA-256 文件。沿用原包名和安装签名以便覆盖更新；文件名称不改变签名类型。

## 部署和验证状态

家庭资料迁移：`202609060003_family_profile.sql`。验证码邮件正文使用 `supabase/templates/recovery.html`，需更新 Supabase Reset Password 模板。整体 config push 被自动审批拒绝（可能改 SMTP/认证配置），电脑操作因无法可靠识别浏览器 URL 停止；用户已按提供步骤保存验证码邮件模板，未修改现有 SMTP；邮件实际投递和完整重置流程仍待使用确认。

2026-09-06：003 家庭资料迁移和 ledger-ai 函数已通过官方 CLI 部署成功。首轮 CI 34038879241 的构建、JVM、模拟器和隔离 PostgreSQL 测试通过；复审追加确认回复写入失败的整体回滚，并处理登录成功但读取家庭失败时的对话归属恢复。最终构建结果在交付时追加。报告 39 项与对话 11 项合成协议回归通过；新增身份、日周计算、Room 迁移/对话持久化与资料权限测试不等于真实邮件/手机验收。

## 最终交付记录

- 代码提交：`5d5a6335515f818e2ece2c1e5f3257cc34508c33`，GitHub 验证签名；最终文档及自动生成的 Room schema 单独补充提交，不改变 APK 源码。
- [最终 CI 34039378934](https://github.com/amateurish-programmer/ai-family-ledger/actions/runs/34039378934)：build、device-tests、backend-tests 全部 success。
- 全部 66 项 JVM 测试通过，0 失败、0 忽略；包含金额、导入、备份、同步、密码恢复和日周边界。
- Android 35 模拟器 12 项通过，0 失败、0 跳过；覆盖 Room 2→3 迁移、退出保留、重开/账号分区、确认幂等及回复写入失败整体回滚。界面流程覆盖角色头像选择、日周切换和打开密码恢复表单；已查看日报、设置、找回密码截图。
- 39 项报告、11 项财务对话合成协议检查，以及隔离 PostgreSQL 家庭权限脚本通过。生产只应用迁移，没有执行测试夹具。
- APK：`dist/apk/AI家庭账本-v0.9.0.apk`，17,745,783 字节，ZIP 完整性和必需 APK 文件检查通过；code9、versionName0.9.0，沿用开发签名。未生成本次 SHA-256 边车文件。
- 本机没有 JDK，Android 构建及测试在 GitHub Actions 完成；本次选择 build_only + auth_checks + device_tests，没有执行 lint。没有进行真实邮件完整重置、真实手机、真实模型响应或家庭多设备刷新验收。

安装：直接覆盖旧版，不要卸载。登录页“忘记密码”发送验证码并设置新密码；设置页编辑角色与头像，“家庭账号与同步”中由创建者编辑家庭名称及图标，其他成员刷新或同步获取。

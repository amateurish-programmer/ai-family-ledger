# AI 家庭账本开发约定

- 使用 Kotlin、Jetpack Compose、Room；minSdk 26；后端路线 Supabase。
- 扩展前读取 docs 中阶段设计和验证记录，区分规划、源码、编译、设备和云端验收。
- 金额为整数分，禁止 Float/Double 参与实际金额计算。图表比例可使用浮点。
- 成员归属与认证操作者分开；未接入 Auth 时不将本地标签作为权限依据。
- LLM 只解析或解释，不能计算报告总额；AI 提案经用户确认后再入账。
- 模型密钥和 service_role 只在服务端；真实账本、备份和生产密钥不进入 Git 或测试夹具。
- 新增数据库版本必须提供迁移，不允许 destructive fallback。
- 历史表格以实际表头与行内容为准；疑似重复不是确定重复；余额调整不算收入。
- 金额、导入、备份、权限和同步逻辑需行为测试。完成前运行能运行的验证并注明未执行部分。
- 每次行为改变更新对应说明；提交和发布只包含经过检查的明确文件清单。
